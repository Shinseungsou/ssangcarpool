<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <EditText
        android:id="@+id/auth_id"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_above="@+id/auth_pw"
        android:layout_centerHorizontal="true"
        android:inputType="text"
        android:hint="@string/auth_label_id" />
    <EditText
        android:id="@+id/auth_pw"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_above="@+id/auth_sign_in"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="24dp"
        android:inputType="textPassword"
        android:hint="@string/auth_label_password" />
    <TextView
        android:id="@+id/auth_sign_in"
        android:layout_width="184dp"
        android:layout_height="32dp"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="8dp"
        android:layout_above="@+id/auth_sign_up"
        android:background="#ff0000"
        android:textColor="#ffffff"
        android:textStyle="bold"
        android:textSize="18sp"
        android:gravity="center"
        android:text="@string/auth_label_sign_in" />
    <TextView
        android:id="@+id/auth_sign_up"
        android:layout_width="184dp"
        android:layout_height="32dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="120dp"
        android:gravity="center"
        android:textSize="18sp"
        android:text="@string/auth_label_sign_up" />
</RelativeLayout>
